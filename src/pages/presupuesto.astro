---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout
  title="Pedir presupuesto — Imprenta Boadilla"
  description="Solicita presupuesto de imprenta en Boadilla del Monte: cantidades, medidas, acabados y fecha. Respuesta rápida."
>
  <section class="mx-auto max-w-3xl px-4 py-14">
    <h1 class="text-3xl font-bold tracking-tight">Pedir presupuesto</h1>
    <p class="mt-2 text-slatebrand-600">Indica cantidades, medidas, acabados y fecha. Cuanta más info, más rápido.</p>

    <div class="mt-8 card p-6">
      <form class="grid gap-3" action="/api/request" method="POST">
        <input type="text" name="company" class="hidden" tabindex="-1" autocomplete="off" />

        <div class="grid gap-3 md:grid-cols-2">
          <input id="p_name" class="input" name="name" required placeholder="Nombre" autocomplete="name" />
          <input id="p_phone" class="input" name="phone" placeholder="Teléfono" autocomplete="tel" inputmode="tel" />
        </div>

        <input id="p_email" class="input" name="email" type="email" required placeholder="Email" autocomplete="email" />

        <select id="p_type" class="input" name="type">
          <option value="imprenta">Imprenta (tarjetas, flyers...)</option>
          <option value="carteleria">Cartelería / rotulación</option>
          <option value="packaging">Packaging / pegatinas</option>
          <option value="diseno">Diseño</option>
          <option value="otro">Otro</option>
        </select>

        <textarea
          id="p_message"
          class="input min-h-[180px]"
          name="message"
          required
          placeholder="Detalles: cantidad, medidas, material/papel, acabado (mate/brillo/laminado), fecha de entrega..."
        ></textarea>

        <input type="hidden" name="source" value="presupuesto" />

        <label class="flex items-start gap-2 text-sm text-slatebrand-600">
          <input class="mt-1" type="checkbox" required />
          <span>He leído y acepto la <a class="underline hover:text-slatebrand-900" href="/legal/privacidad">política de privacidad</a>.</span>
        </label>

        <button class="btn btn-primary px-5 py-3" type="submit">Enviar</button>
      </form>
    </div>

    <div class="mt-6 rounded-3xl border border-warm-200 bg-warm-50 p-6">
      <p class="font-bold text-warm-900">Tip rápido</p>
      <p class="mt-2 text-sm text-warm-900/80">PDF a tamaño final + sangrado, resolución mínima 300dpi.</p>
    </div>
  </section>
</BaseLayout>
